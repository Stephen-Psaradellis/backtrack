# Backtrack - Git Ignore Configuration

# Auto Claude data directory
.auto-claude/

.worktrees/

# Environment variables (NEVER commit secrets)
.env*
!.env.example

# Dependencies
/node_modules
**/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Downloaded assets (large files, fetch from source)
assets/downloads/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Next.js
/.next/
/out/

# Native builds
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# iOS build artifacts (redundant since ios/ is ignored, but explicit for safety)
*.ipa
*.dSYM.zip
*.dSYM

# Android - ENTIRE DIRECTORY (CNG approach - regenerated via expo prebuild)
android/

# Android build artifacts (redundant but explicit)
*.apk
*.aab
local.properties

# iOS - ENTIRE DIRECTORY (CNG approach - regenerated via expo prebuild)
ios/

# Production
/build

# Metro bundler
.metro-health-check*

# Testing
/coverage
.jest/
e2e/artifacts/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# TypeScript
*.tsbuildinfo

# Misc
*.pem

# Vercel
.vercel

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
.cache/

# E2E test screenshots (generated during testing)
e2e_screenshots/
screen.png
nul

# Task planning files (Claude working documents)
tasks/
# Credentials and secrets (NEVER commit)
credentials.json
google-services.json
GoogleService-Info.plist

# Supabase temp files (may contain connection strings)
supabase/.temp/

# E2E seed scripts that might contain secrets (use env vars instead)
# scripts/seed-e2e-data.mjs  # Tracked but secrets removed - use doppler run

# Strawberry-Claude (separate project, not part of main app)
strawberry-claude/
